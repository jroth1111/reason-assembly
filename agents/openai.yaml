interface:
  display_name: "Model Council"
  short_description: "Run explicit multi-model councils and reviews"
  default_prompt: "Use $model-council to run an evidence-backed decision council for this task."
policy:
  allow_implicit_invocation: false
