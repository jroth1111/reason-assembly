interface:
  display_name: "Reason Assembly"
  short_description: "Run explicit evidence-backed agent councils and reviews"
  default_prompt: "Use $reason-assembly to run an evidence-backed agent council for this task."
policy:
  allow_implicit_invocation: false
